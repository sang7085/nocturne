import { useEffect, useRef } from "react";
import { gsap } from "gsap";
import Image from "next/image";


export default function FooterSec({ loading, loopY }) {
  useEffect(() => {
    if(!loading) {
      
    }
  }, []);
  return(
      <>
        <section className="footer_sec"></section>
      </>
  )
}